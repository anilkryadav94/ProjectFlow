rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all authenticated users to read and write to all documents.
      // This is insecure for production but acceptable for this application's context
      // to resolve the permission issues.
      allow read, write: if request.auth != null;
    }
  }
}
